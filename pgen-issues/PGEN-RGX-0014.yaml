id: PGEN-RGX-0014
summary: |
  CLOSED — NOT A PGEN BUG. PGEN correctly parses \X as a simple_escape
  node. The error was in RGX's parsing.rs adapter which did not handle
  'X' in its simple_escape character mapping.
status: closed
opened_at: 2026-04-08T00:00:00Z
first_seen_at: 2026-04-08T00:00:00Z
last_updated_at: 2026-04-08T00:00:00Z
parser_backend: pgen
parser_backend_version: "1.1.8"
rgx_commit: 0f60722
upstream_report:
  reported: false
  issue_id: null
  issue_url: null
  reported_at: null
context:
  feature_flag: pgen-parser
  parser_entrypoint: rgx-core/src/parsing.rs
  command: "cargo run --bin rgx-cli -- '\\X' 'cafe'"
  pattern: "\\X"
  input: "cafe"
  notes: |
    PGEN produces a correct simple_escape AST node for \X.
    The error was in RGX's simple_escape handler in parsing.rs
    which did not map 'X' to Regex::GraphemeCluster.
expected_behavior: |
  N/A — PGEN behaves correctly.
actual_behavior: |
  N/A — PGEN behaves correctly. RGX adapter was missing the mapping.
reproduction: |
  PGEN parseability_probe confirms successful parse:
    cargo run --bin parseability_probe -- --parse regex repro_input.txt --profile regex_default
    => "parse_full passed"
  AST dump shows: rule_name "simple_escape" at span 1..2
impact: |
  None — this was an RGX-side bug, not a PGEN issue.
resolution:
  status: closed-not-a-pgen-bug
  fixed_in_rgx_commit: null
  verified_at: 2026-04-08T00:00:00Z
  verification_notes: |
    Verified via parseability_probe that PGEN correctly parses \X.
    Fix will be applied in RGX parsing.rs adapter.
